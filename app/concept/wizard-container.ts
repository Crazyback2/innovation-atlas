/** Contenitore 890px condiviso dai wizard autenticati (CFML, …). */
export const wizardContainerClassName =
  "mx-auto w-full min-w-0 max-w-wizard overflow-x-clip px-6 lg:px-0";

/**
 * Blocco 1160px della vista rispondente SP (pack, passi, preview, grazie).
 * Stessa larghezza di ConceptHero; il centraggio è sul wrapper pagina.
 */
export const respondentContainerClassName = "w-[1160px] max-w-full";

/** Centra il blocco rispondente come la pagina concept (Cubit). */
export const respondentPageClassName =
  "mx-auto flex w-full max-w-[1440px] flex-col items-center px-[var(--spacing-gutter)]";
